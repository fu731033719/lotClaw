{
  "name": "@lotclaw/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-test-isolation=none dist/**/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lotclaw/agent-core": "file:../../packages/agent-core",
    "@lotclaw/agent-tools": "file:../../packages/agent-tools"
  }
}
